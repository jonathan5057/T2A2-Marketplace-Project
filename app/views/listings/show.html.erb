<p id="notice"><%= notice %></p>

<p>
  <strong>Category:</strong>
  <%= @listing.category %>
</p>

<p>
  <strong>Title:</strong>
  <%= @listing.title %>
</p>

<p>
  <strong>Quantity:</strong>
  <%= @listing.quantity %>
</p>

<p>
  <strong>Price:</strong>
  <%= @listing.price %>
</p>

<p>
  <strong>Vendor:</strong>
  <%= @listing.user.username %>
</p>

<p>
  <strong>Distribution Centre:</strong>
  <%= @listing.warehouse.full_address %>
</p>



<% if user_signed_in? and (current_user.admin? or current_user.id==@listing.user_id) %>
  <%= link_to 'Edit', edit_listing_path(@listing) %> |
  <%= link_to 'Destroy', @listing, method: :delete, data: { confirm: 'Are you sure?' } %> |
<% end %>

<%= link_to 'Back', listings_path %>
